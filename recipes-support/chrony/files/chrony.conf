# Welcome to the chrony configuration file. See chrony.conf(5) for more
# information about usable directives.

# Use time sources from DHCP.
sourcedir /run/chrony-dhcp

# Use NTP sources found in /etc/chrony/sources.d.
sourcedir /etc/chrony/sources.d

# This directive specifies the location of the file containing ID/key pairs for
# NTP authentication.
keyfile /etc/chrony/chrony.keys

# This directive specifies the file into which chronyd will store the rate
# information.
driftfile /var/lib/chrony/chrony.drift

# Save NTS keys and cookies.
ntsdumpdir /var/lib/chrony

# Uncomment the following line to turn logging on.
#log tracking measurements statistics

# Log files location.
logdir /var/log/chrony

# Stop bad estimates upsetting machine clock.
maxupdateskew 100.0

# Step the system clock instead of slewing it if the adjustment is larger than
# one second, but only in the first three clock updates.
makestep 1.0 -1
minsamples 2
maxsamples 4

# Sync hardware clock.
rtcsync

# Get TAI-UTC offset and leap seconds from the system tz database.
# Keep this disabled for compatibility with chronyd builds that don't
# support the directive or don't ship the leap-seconds list.
#leapseclist /usr/share/zoneinfo/leap-seconds.list

# Include configuration files found in /etc/chrony/conf.d.
confdir /etc/chrony/conf.d

# Primary (University server)
server engagesrv1.lutz.louisville.edu iburst prefer minpoll 1 maxpoll 4

# Backup servers (used automatically if primary fails)
server time.google.com iburst minpoll 3 maxpoll 4
server time.cloudflare.com iburst minpoll 3 maxpoll 4
pool pool.ntp.org iburst minpoll 3 maxpoll 4
