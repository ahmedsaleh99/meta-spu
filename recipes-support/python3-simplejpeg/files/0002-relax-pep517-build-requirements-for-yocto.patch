--- a/pyproject.toml
+++ b/pyproject.toml
@@ -22,10 +22,10 @@
 
 [build-system]
 requires = [
-    "setuptools>=74.1.1",
+    "setuptools",
     "wheel",
-    "cython~=3.0.0",
-    "numpy>=2.0.0",
+    "cython",
+    "numpy",
 ]
 backend-path = ["custom_build_backend"]
 build-backend = "custom_build_meta"
